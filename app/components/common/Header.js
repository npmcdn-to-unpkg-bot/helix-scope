import React from 'react';
import NavLink from './NavLink';

const Header = props => {
  return (
    <div>
      <header className="l-header">
      </header>
    </div>
  );
};

export default Header;
